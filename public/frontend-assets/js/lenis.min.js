function t(h, a, c) {
    return Math.max(h, Math.min(a, c))
}
var e = class {
        isRunning = !1;
        value = 0;
        from = 0;
        to = 0;
        currentTime = 0;
        lerp;
        duration;
        easing;
        onUpdate;
        advance(h) {
            if (!this.isRunning) return;
            let a = !1;
            if (this.duration && this.easing) {
                this.currentTime += h;
                let c = t(0, this.currentTime / this.duration, 1);
                a = c >= 1;
                let p = a ? 1 : this.easing(c);
                this.value = this.from + (this.to - this.from) * p
            } else {
                var d, u, m, v, g, S, f;
                this.lerp ? (this.value = (d = this.value, u = this.to, m = 60 * this.lerp, v = h, g = d, S = u, (1 - (f = 1 - Math.exp(-m * v))) * g + f * S), Math.round(this.value) === this.to && (this.value = this.to, a = !0)) : (this.value = this.to, a = !0)
            }
            a && this.stop(), this.onUpdate ? .(this.value, a)
        }
        stop() {
            this.isRunning = !1
        }
        fromTo(h, a, {
            lerp: c,
            duration: p,
            easing: d,
            onStart: u,
            onUpdate: m
        }) {
            this.from = this.value = h, this.to = a, this.lerp = c, this.duration = p, this.easing = d, this.currentTime = 0, this.isRunning = !0, u ? .(), this.onUpdate = m
        }
    },
    i = class {
        constructor(h, a, {
            autoResize: c = !0,
            debounce: p = 250
        } = {}) {
            this.wrapper = h, this.content = a, c && (this.debouncedResize = function(h, a) {
                let c;
                return function(...p) {
                    let d = this;
                    clearTimeout(c), c = setTimeout(() => {
                        c = void 0, h.apply(d, p)
                    }, a)
                }
            }(this.resize, p), this.wrapper instanceof Window ? window.addEventListener("resize", this.debouncedResize, !1) : (this.wrapperResizeObserver = new ResizeObserver(this.debouncedResize), this.wrapperResizeObserver.observe(this.wrapper)), this.contentResizeObserver = new ResizeObserver(this.debouncedResize), this.contentResizeObserver.observe(this.content)), this.resize()
        }
        width = 0;
        height = 0;
        scrollHeight = 0;
        scrollWidth = 0;
        debouncedResize;
        wrapperResizeObserver;
        contentResizeObserver;
        destroy() {
            this.wrapperResizeObserver ? .disconnect(), this.contentResizeObserver ? .disconnect(), this.wrapper === window && this.debouncedResize && window.removeEventListener("resize", this.debouncedResize, !1)
        }
        resize = () => {
            this.onWrapperResize(), this.onContentResize()
        };
        onWrapperResize = () => {
            this.wrapper instanceof Window ? (this.width = window.innerWidth, this.height = window.innerHeight) : (this.width = this.wrapper.clientWidth, this.height = this.wrapper.clientHeight)
        };
        onContentResize = () => {
            this.wrapper instanceof Window ? (this.scrollHeight = this.content.scrollHeight, this.scrollWidth = this.content.scrollWidth) : (this.scrollHeight = this.wrapper.scrollHeight, this.scrollWidth = this.wrapper.scrollWidth)
        };
        get limit() {
            return {
                x: this.scrollWidth - this.width,
                y: this.scrollHeight - this.height
            }
        }
    },
    s = class {
        events = {};
        emit(h, ...a) {
            let c = this.events[h] || [];
            for (let p = 0, d = c.length; p < d; p++) c[p] ? .(...a)
        }
        on(h, a) {
            return this.events[h] ? .push(a) || (this.events[h] = [a]), () => {
                this.events[h] = this.events[h] ? .filter(h => a !== h)
            }
        }
        off(h, a) {
            this.events[h] = this.events[h] ? .filter(h => a !== h)
        }
        destroy() {
            this.events = {}
        }
    },
    o = 100 / 6,
    n = {
        passive: !1
    },
    r = class {
        constructor(h, a = {
            wheelMultiplier: 1,
            touchMultiplier: 1
        }) {
            this.element = h, this.options = a, window.addEventListener("resize", this.onWindowResize, !1), this.onWindowResize(), this.element.addEventListener("wheel", this.onWheel, n), this.element.addEventListener("touchstart", this.onTouchStart, n), this.element.addEventListener("touchmove", this.onTouchMove, n), this.element.addEventListener("touchend", this.onTouchEnd, n)
        }
        touchStart = {
            x: 0,
            y: 0
        };
        lastDelta = {
            x: 0,
            y: 0
        };
        window = {
            width: 0,
            height: 0
        };
        emitter = new s;
        on(h, a) {
            return this.emitter.on(h, a)
        }
        destroy() {
            this.emitter.destroy(), window.removeEventListener("resize", this.onWindowResize, !1), this.element.removeEventListener("wheel", this.onWheel, n), this.element.removeEventListener("touchstart", this.onTouchStart, n), this.element.removeEventListener("touchmove", this.onTouchMove, n), this.element.removeEventListener("touchend", this.onTouchEnd, n)
        }
        onTouchStart = h => {
            let {
                clientX: a,
                clientY: c
            } = h.targetTouches ? h.targetTouches[0] : h;
            this.touchStart.x = a, this.touchStart.y = c, this.lastDelta = {
                x: 0,
                y: 0
            }, this.emitter.emit("scroll", {
                deltaX: 0,
                deltaY: 0,
                event: h
            })
        };
        onTouchMove = h => {
            let {
                clientX: a,
                clientY: c
            } = h.targetTouches ? h.targetTouches[0] : h, p = -(a - this.touchStart.x) * this.options.touchMultiplier, d = -(c - this.touchStart.y) * this.options.touchMultiplier;
            this.touchStart.x = a, this.touchStart.y = c, this.lastDelta = {
                x: p,
                y: d
            }, this.emitter.emit("scroll", {
                deltaX: p,
                deltaY: d,
                event: h
            })
        };
        onTouchEnd = h => {
            this.emitter.emit("scroll", {
                deltaX: this.lastDelta.x,
                deltaY: this.lastDelta.y,
                event: h
            })
        };
        onWheel = h => {
            let {
                deltaX: a,
                deltaY: c,
                deltaMode: p
            } = h;
            a *= 1 === p ? o : 2 === p ? this.window.width : 1, c *= 1 === p ? o : 2 === p ? this.window.height : 1, a *= this.options.wheelMultiplier, c *= this.options.wheelMultiplier, this.emitter.emit("scroll", {
                deltaX: a,
                deltaY: c,
                event: h
            })
        };
        onWindowResize = () => {
            this.window = {
                width: window.innerWidth,
                height: window.innerHeight
            }
        }
    },
    l = h => Math.min(1, 1.001 - Math.pow(2, -10 * h)),
    Lenis = class {
        _isScrolling = !1;
        _isStopped = !1;
        _isLocked = !1;
        _preventNextNativeScrollEvent = !1;
        _resetVelocityTimeout = null;
        __rafID = null;
        isTouching;
        time = 0;
        userData = {};
        lastVelocity = 0;
        velocity = 0;
        direction = 0;
        options;
        targetScroll;
        animatedScroll;
        animate = new e;
        emitter = new s;
        dimensions;
        virtualScroll;
        constructor({
            wrapper: h = window,
            content: a = document.documentElement,
            eventsTarget: c = h,
            smoothWheel: p = !0,
            syncTouch: d = !1,
            syncTouchLerp: u = .075,
            touchInertiaMultiplier: m = 35,
            duration: v,
            easing: g,
            lerp: S = .1,
            infinite: f = !1,
            orientation: w = "vertical",
            gestureOrientation: y = "vertical",
            touchMultiplier: $ = 1,
            wheelMultiplier: _ = 1,
            autoResize: E = !0,
            prevent: T,
            virtualScroll: z,
            overscroll: b = !0,
            autoRaf: L = !1,
            anchors: N = !1,
            autoToggle: R = !1,
            allowNestedScroll: W = !1,
            __experimental__naiveDimensions: x = !1
        } = {}) {
            window.lenisVersion = "1.3.4", h && h !== document.documentElement || (h = window), "number" == typeof v && "function" != typeof g ? g = l : "function" == typeof g && "number" != typeof v && (v = 1), this.options = {
                wrapper: h,
                content: a,
                eventsTarget: c,
                smoothWheel: p,
                syncTouch: d,
                syncTouchLerp: u,
                touchInertiaMultiplier: m,
                duration: v,
                easing: g,
                lerp: S,
                infinite: f,
                gestureOrientation: y,
                orientation: w,
                touchMultiplier: $,
                wheelMultiplier: _,
                autoResize: E,
                prevent: T,
                virtualScroll: z,
                overscroll: b,
                autoRaf: L,
                anchors: N,
                autoToggle: R,
                allowNestedScroll: W,
                __experimental__naiveDimensions: x
            }, this.dimensions = new i(h, a, {
                autoResize: E
            }), this.updateClassName(), this.targetScroll = this.animatedScroll = this.actualScroll, this.options.wrapper.addEventListener("scroll", this.onNativeScroll, !1), this.options.wrapper.addEventListener("scrollend", this.onScrollEnd, {
                capture: !0
            }), this.options.anchors && this.options.wrapper === window && this.options.wrapper.addEventListener("click", this.onClick, !1), this.options.wrapper.addEventListener("pointerdown", this.onPointerDown, !1), this.virtualScroll = new r(c, {
                touchMultiplier: $,
                wheelMultiplier: _
            }), this.virtualScroll.on("scroll", this.onVirtualScroll), this.options.autoToggle && this.rootElement.addEventListener("transitionend", this.onTransitionEnd, {
                passive: !0
            }), this.options.autoRaf && (this.__rafID = requestAnimationFrame(this.raf))
        }
        destroy() {
            this.emitter.destroy(), this.options.wrapper.removeEventListener("scroll", this.onNativeScroll, !1), this.options.wrapper.removeEventListener("scrollend", this.onScrollEnd, {
                capture: !0
            }), this.options.wrapper.removeEventListener("pointerdown", this.onPointerDown, !1), this.options.anchors && this.options.wrapper === window && this.options.wrapper.removeEventListener("click", this.onClick, !1), this.virtualScroll.destroy(), this.dimensions.destroy(), this.cleanUpClassName(), this.__rafID && cancelAnimationFrame(this.__rafID)
        }
        on(h, a) {
            return this.emitter.on(h, a)
        }
        off(h, a) {
            return this.emitter.off(h, a)
        }
        onScrollEnd = h => {
            h instanceof CustomEvent || "smooth" !== this.isScrolling && !1 !== this.isScrolling || h.stopPropagation()
        };
        dispatchScrollendEvent = () => {
            this.options.wrapper.dispatchEvent(new CustomEvent("scrollend", {
                bubbles: this.options.wrapper === window,
                detail: {
                    lenisScrollEnd: !0
                }
            }))
        };
        onTransitionEnd = h => {
            if (h.propertyName.includes("overflow")) {
                let a = this.isHorizontal ? "overflow-x" : "overflow-y",
                    c = getComputedStyle(this.rootElement)[a];
                ["hidden", "clip"].includes(c) ? this.stop() : this.start()
            }
        };
        setScroll(h) {
            this.isHorizontal ? this.options.wrapper.scrollTo({
                left: h,
                behavior: "instant"
            }) : this.options.wrapper.scrollTo({
                top: h,
                behavior: "instant"
            })
        }
        onClick = h => {
            let a = h.composedPath().find(h => h instanceof HTMLAnchorElement && (h.getAttribute("href") ? .startsWith("#") || h.getAttribute("href") ? .startsWith("/#") || h.getAttribute("href") ? .startsWith("./#")));
            if (a) {
                let c = a.getAttribute("href");
                if (c) {
                    let p = "object" == typeof this.options.anchors && this.options.anchors ? this.options.anchors : void 0,
                        d = `#${c.split("#")[1]}`;
                    ["#", "/#", "./#", "#top", "/#top", "./#top"].includes(c) && (d = 0), this.scrollTo(d, p)
                }
            }
        };
        onPointerDown = h => {
            1 === h.button && this.reset()
        };
        onVirtualScroll = h => {
            if ("function" == typeof this.options.virtualScroll && !1 === this.options.virtualScroll(h)) return;
            let {
                deltaX: a,
                deltaY: c,
                event: p
            } = h;
            if (this.emitter.emit("virtual-scroll", {
                    deltaX: a,
                    deltaY: c,
                    event: p
                }), p.ctrlKey || p.lenisStopPropagation) return;
            let d = p.type.includes("touch"),
                u = p.type.includes("wheel");
            this.isTouching = "touchstart" === p.type || "touchmove" === p.type;
            let m = 0 === a && 0 === c;
            if (this.options.syncTouch && d && "touchstart" === p.type && m && !this.isStopped && !this.isLocked) return void this.reset();
            let v = "vertical" === this.options.gestureOrientation && 0 === c || "horizontal" === this.options.gestureOrientation && 0 === a;
            if (m || v) return;
            let g = p.composedPath();
            g = g.slice(0, g.indexOf(this.rootElement));
            let S = this.options.prevent;
            if (g.find(h => h instanceof HTMLElement && ("function" == typeof S && S ? .(h) || h.hasAttribute ? .("data-lenis-prevent") || d && h.hasAttribute ? .("data-lenis-prevent-touch") || u && h.hasAttribute ? .("data-lenis-prevent-wheel") || this.options.allowNestedScroll && this.checkNestedScroll(h, {
                    deltaX: a,
                    deltaY: c
                })))) return;
            if (this.isStopped || this.isLocked) return void p.preventDefault();
            if (!(this.options.syncTouch && d || this.options.smoothWheel && u)) return this.isScrolling = "native", this.animate.stop(), void(p.lenisStopPropagation = !0);
            let f = c;
            "both" === this.options.gestureOrientation ? f = Math.abs(c) > Math.abs(a) ? c : a : "horizontal" === this.options.gestureOrientation && (f = a), (!this.options.overscroll || this.options.infinite || this.options.wrapper !== window && (this.animatedScroll > 0 && this.animatedScroll < this.limit || 0 === this.animatedScroll && c > 0 || this.animatedScroll === this.limit && c < 0)) && (p.lenisStopPropagation = !0), p.preventDefault();
            let w = d && this.options.syncTouch,
                y = d && "touchend" === p.type && Math.abs(f) > 5;
            y && (f = this.velocity * this.options.touchInertiaMultiplier), this.scrollTo(this.targetScroll + f, {
                programmatic: !1,
                ...w ? {
                    lerp: y ? this.options.syncTouchLerp : 1
                } : {
                    lerp: this.options.lerp,
                    duration: this.options.duration,
                    easing: this.options.easing
                }
            })
        };
        resize() {
            this.dimensions.resize(), this.animatedScroll = this.targetScroll = this.actualScroll, this.emit()
        }
        emit() {
            this.emitter.emit("scroll", this)
        }
        onNativeScroll = () => {
            if (null !== this._resetVelocityTimeout && (clearTimeout(this._resetVelocityTimeout), this._resetVelocityTimeout = null), this._preventNextNativeScrollEvent) this._preventNextNativeScrollEvent = !1;
            else if (!1 === this.isScrolling || "native" === this.isScrolling) {
                let h = this.animatedScroll;
                this.animatedScroll = this.targetScroll = this.actualScroll, this.lastVelocity = this.velocity, this.velocity = this.animatedScroll - h, this.direction = Math.sign(this.animatedScroll - h), this.isStopped || (this.isScrolling = "native"), this.emit(), 0 !== this.velocity && (this._resetVelocityTimeout = setTimeout(() => {
                    this.lastVelocity = this.velocity, this.velocity = 0, this.isScrolling = !1, this.emit()
                }, 400))
            }
        };
        reset() {
            this.isLocked = !1, this.isScrolling = !1, this.animatedScroll = this.targetScroll = this.actualScroll, this.lastVelocity = this.velocity = 0, this.animate.stop()
        }
        start() {
            this.isStopped && (this.reset(), this.isStopped = !1, this.emit())
        }
        stop() {
            this.isStopped || (this.reset(), this.isStopped = !0, this.emit())
        }
        raf = h => {
            let a = h - (this.time || h);
            this.time = h, this.animate.advance(.001 * a), this.options.autoRaf && (this.__rafID = requestAnimationFrame(this.raf))
        };
        scrollTo(h, {
            offset: a = 0,
            immediate: c = !1,
            lock: p = !1,
            duration: d = this.options.duration,
            easing: u = this.options.easing,
            lerp: m = this.options.lerp,
            onStart: v,
            onComplete: g,
            force: S = !1,
            programmatic: f = !0,
            userData: w
        } = {}) {
            if (!this.isStopped && !this.isLocked || S) {
                if ("string" == typeof h && ["top", "left", "start"].includes(h)) h = 0;
                else if ("string" == typeof h && ["bottom", "right", "end"].includes(h)) h = this.limit;
                else {
                    let y;
                    if ("string" == typeof h ? y = document.querySelector(h) : h instanceof HTMLElement && h ? .nodeType && (y = h), y) {
                        if (this.options.wrapper !== window) {
                            let $ = this.rootElement.getBoundingClientRect();
                            a -= this.isHorizontal ? $.left : $.top
                        }
                        let _ = y.getBoundingClientRect();
                        h = (this.isHorizontal ? _.left : _.top) + this.animatedScroll
                    }
                }
                if ("number" == typeof h) {
                    if (h += a, h = Math.round(h), this.options.infinite) {
                        if (f) {
                            this.targetScroll = this.animatedScroll = this.scroll;
                            let E = h - this.animatedScroll;
                            E > this.limit / 2 ? h -= this.limit : E < -this.limit / 2 && (h += this.limit)
                        }
                    } else h = t(0, h, this.limit);
                    if (h === this.targetScroll) return v ? .(this), void g ? .(this);
                    if (this.userData = w ? ? {}, c) return this.animatedScroll = this.targetScroll = h, this.setScroll(this.scroll), this.reset(), this.preventNextNativeScrollEvent(), this.emit(), g ? .(this), this.userData = {}, void requestAnimationFrame(() => {
                        this.dispatchScrollendEvent()
                    });
                    f || (this.targetScroll = h), "number" == typeof d && "function" != typeof u ? u = l : "function" == typeof u && "number" != typeof d && (d = 1), this.animate.fromTo(this.animatedScroll, h, {
                        duration: d,
                        easing: u,
                        lerp: m,
                        onStart: () => {
                            p && (this.isLocked = !0), this.isScrolling = "smooth", v ? .(this)
                        },
                        onUpdate: (h, a) => {
                            this.isScrolling = "smooth", this.lastVelocity = this.velocity, this.velocity = h - this.animatedScroll, this.direction = Math.sign(this.velocity), this.animatedScroll = h, this.setScroll(this.scroll), f && (this.targetScroll = h), a || this.emit(), a && (this.reset(), this.emit(), g ? .(this), this.userData = {}, requestAnimationFrame(() => {
                                this.dispatchScrollendEvent()
                            }), this.preventNextNativeScrollEvent())
                        }
                    })
                }
            }
        }
        preventNextNativeScrollEvent() {
            this._preventNextNativeScrollEvent = !0, requestAnimationFrame(() => {
                this._preventNextNativeScrollEvent = !1
            })
        }
        checkNestedScroll(h, {
            deltaX: a,
            deltaY: c
        }) {
            let p = Date.now(),
                d = h._lenis ? ? = {},
                u, m, v, g, S, f, w, y, $ = this.options.gestureOrientation;
            if (p - (d.time ? ? 0) > 2e3) {
                d.time = Date.now();
                let _ = window.getComputedStyle(h);
                d.computedStyle = _;
                let E = _.overflowX,
                    T = _.overflowY;
                if (u = ["auto", "overlay", "scroll"].includes(E), m = ["auto", "overlay", "scroll"].includes(T), d.hasOverflowX = u, d.hasOverflowY = m, !u && !m || "vertical" === $ && !m || "horizontal" === $ && !u) return !1;
                S = h.scrollWidth, f = h.scrollHeight, w = h.clientWidth, y = h.clientHeight, v = S > w, g = f > y, d.isScrollableX = v, d.isScrollableY = g, d.scrollWidth = S, d.scrollHeight = f, d.clientWidth = w, d.clientHeight = y
            } else v = d.isScrollableX, g = d.isScrollableY, u = d.hasOverflowX, m = d.hasOverflowY, S = d.scrollWidth, f = d.scrollHeight, w = d.clientWidth, y = d.clientHeight;
            if (!u && !m || !v && !g || !("vertical" !== $ || m && g) || !("horizontal" !== $ || u && v)) return !1;
            let z, b, L, N, R, W;
            if ("horizontal" === $ ? z = "x" : "vertical" === $ ? z = "y" : (0 !== a && u && v && (z = "x"), 0 !== c && m && g && (z = "y")), !z) return !1;
            if ("x" === z) b = h.scrollLeft, L = S - w, N = a, R = u, W = v;
            else {
                if ("y" !== z) return !1;
                b = h.scrollTop, L = f - y, N = c, R = m, W = g
            }
            return (N > 0 ? b < L : b > 0) && R && W
        }
        get rootElement() {
            return this.options.wrapper === window ? document.documentElement : this.options.wrapper
        }
        get limit() {
            return this.options.__experimental__naiveDimensions ? this.isHorizontal ? this.rootElement.scrollWidth - this.rootElement.clientWidth : this.rootElement.scrollHeight - this.rootElement.clientHeight : this.dimensions.limit[this.isHorizontal ? "x" : "y"]
        }
        get isHorizontal() {
            return "horizontal" === this.options.orientation
        }
        get actualScroll() {
            let h = this.options.wrapper;
            return this.isHorizontal ? h.scrollX ? ? h.scrollLeft : h.scrollY ? ? h.scrollTop
        }
        get scroll() {
            var h, a;
            return this.options.infinite ? ((h = this.animatedScroll) % (a = this.limit) + a) % a : this.animatedScroll
        }
        get progress() {
            return 0 === this.limit ? 1 : this.scroll / this.limit
        }
        get isScrolling() {
            return this._isScrolling
        }
        set isScrolling(h) {
            this._isScrolling !== h && (this._isScrolling = h, this.updateClassName())
        }
        get isStopped() {
            return this._isStopped
        }
        set isStopped(h) {
            this._isStopped !== h && (this._isStopped = h, this.updateClassName())
        }
        get isLocked() {
            return this._isLocked
        }
        set isLocked(h) {
            this._isLocked !== h && (this._isLocked = h, this.updateClassName())
        }
        get isSmooth() {
            return "smooth" === this.isScrolling
        }
        get className() {
            let h = "lenis";
            return this.options.autoToggle && (h += " lenis-autoToggle"), this.isStopped && (h += " lenis-stopped"), this.isLocked && (h += " lenis-locked"), this.isScrolling && (h += " lenis-scrolling"), "smooth" === this.isScrolling && (h += " lenis-smooth"), h
        }
        updateClassName() {
            this.cleanUpClassName(), this.rootElement.className = `${this.rootElement.className} ${this.className}`.trim()
        }
        cleanUpClassName() {
            this.rootElement.className = this.rootElement.className.replace(/lenis(-\w+)?/g, "").trim()
        }
    };
globalThis.Lenis = Lenis, globalThis.Lenis.prototype = Lenis.prototype;